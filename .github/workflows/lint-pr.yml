---
name: Lint PR
on:
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize

jobs:
  trigger:
    uses: statnett/github-workflows/.github/workflows/lint-pr.yaml@main
    permissions:
      contents: read
      pull-requests: write
      statuses: write
