---
name: Main
on:
  push:
    branches:
      - main
    tags:
      - v*

jobs:
  build-push-image:
    if: ${{ github.repository == 'statnett/k3a-lag-exporter' }}
    permissions:
      packages: write
    uses: ./.github/workflows/build-image.yml
    with:
      push: true
