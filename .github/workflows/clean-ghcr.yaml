---
name: Delete Obsolete GHCR Images
on:
  schedule:
    - cron: "0 1 * * *"  # every day at midnight
  workflow_dispatch:

permissions:
  contents: read
jobs:
  trigger:
    uses: statnett/github-workflows/.github/workflows/clean-ghcr.yaml@e6814bd0c39d968bfd896cc5ae6de46fb055388e # main
    secrets: inherit
    permissions:
      packages: write
