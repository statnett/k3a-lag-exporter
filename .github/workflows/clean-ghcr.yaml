---
name: Delete Obsolete GHCR Images
on:
  schedule:
    - cron: "0 1 * * *"  # every day at midnight
  workflow_dispatch:

permissions:
  contents: read
jobs:
  trigger:
    uses: statnett/github-workflows/.github/workflows/clean-ghcr.yaml@6d4a134582c00edf0d1dfc1b297c4b8aeb775702 # main
    secrets: inherit
    permissions:
      packages: write
